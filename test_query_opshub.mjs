import { createClient } from '@libsql/client'; const turso = createClient({url: process.env.VITE_TURSO_DB_URL, authToken: process.env.VITE_TURSO_DB_AUTH_TOKEN}); async function main() { try { const rs = await turso.execute("SELECT First_Name, Middle_Name, Last_Name, Email, Role, emp_stat, Status FROM User_Permissions WHERE IFNULL(Status, '') != 'Inactive'"); console.log(rs.rows); } catch (err) { console.error(err); } } main();
